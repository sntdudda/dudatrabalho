import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Navbar from "@/components/navbar"
import Footer from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Toque dos Sonhos - Sua Loja dos Sonhos Online",
  description:
    "Encontre tudo o que você precisa para realizar seus sonhos em nossa loja online. Qualidade, variedade e os melhores preços!",
  keywords: "ecommerce, online shopping, products, deals, fashion, electronics",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
